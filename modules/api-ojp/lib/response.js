
const xmlbuilder = require('xmlbuilder');
const _ = require('lodash');

module.exports = {
  'createResponse': (stops, startTime, ptModes) => {

  },

  'createErrorResponse': (errorCode, startTime) => {

  }
}